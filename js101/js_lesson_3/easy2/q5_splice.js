let numbers = [1, 2, 3, 4, 5];
numbers.splice(2, 1);
console.log(numbers);

// Splice takes in two arguments,
// first: starting index of the element to remove
// second: how many elements to remove from the starting index
