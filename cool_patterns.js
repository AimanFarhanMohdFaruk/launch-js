// Get each digit from a number from last to first
/*
  result = []
  number = 3421
  do {
    let remainder = number % 10;

    number = Math.floor(number / 10);
    result.push(remainder);
  } while (number > 0);

  result => [1,2,4,3]
*/
